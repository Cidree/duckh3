% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3_strings_to.R
\name{ddbh3_strings_to}
\alias{ddbh3_strings_to}
\alias{ddbh3_strings_to_bigint}
\alias{ddbh3_strings_to_spatial}
\alias{ddbh3_strings_to_lon}
\alias{ddbh3_strings_to_lat}
\title{Convert H3 string indexes to other representations}
\usage{
ddbh3_strings_to_bigint(
  x,
  h3string = "h3string",
  conn = NULL,
  name = NULL,
  new_column = "h3bigint",
  overwrite = FALSE,
  quiet = FALSE
)

ddbh3_strings_to_lon(
  x,
  h3string = "h3string",
  conn = NULL,
  name = NULL,
  new_column = "lon",
  overwrite = FALSE,
  quiet = FALSE
)

ddbh3_strings_to_lat(
  x,
  h3string = "h3string",
  conn = NULL,
  name = NULL,
  new_column = "lat",
  overwrite = FALSE,
  quiet = FALSE
)

ddbh3_strings_to_spatial(
  x,
  h3string = "h3string",
  conn = NULL,
  name = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{x}{Input spatial data. Can be:
\itemize{
\item A \code{duckspatial_df} object (lazy spatial data frame via dbplyr)
\item An \code{sf} object
\item A \code{tbl_lazy} from dbplyr
\item A character string naming a table/view in \code{conn}
}
Data is returned from this object.}

\item{h3string}{The name of a column in \code{x} containing the H3 strings}

\item{conn}{A connection object to a DuckDB database. If \code{NULL}, the function
runs on a temporary DuckDB database.}

\item{name}{A character string of length one specifying the name of the table,
or a character string of length two specifying the schema and table
names. If \code{NULL} (the default), the function returns the result as an
\code{sf} object}

\item{new_column}{Name of the new column to create on the input data. If NULL,
the function will return a vector with the result}

\item{overwrite}{Boolean. whether to overwrite the existing table if it exists. Defaults
to \code{FALSE}. This argument is ignored when \code{name} is \code{NULL}.}

\item{quiet}{A logical value. If \code{TRUE}, suppresses any informational messages.
Defaults to \code{FALSE}.}
}
\value{
A \code{tbl_lazy} if \code{x} is not spatial, or a \code{duckspatial_df} if \code{x}
is spatial (e.g. \code{sf} or \code{duckspatial_df}). Alternatively, it creates a table in the connection
if \code{name} is provided, and returns \code{TRUE} invisibly.
}
\description{
Convert H3 cell indexes stored as strings into other representations:
\code{ddbh3_strings_to_bigint()}, \code{ddbh3_strings_to_spatial()},
\code{ddbh3_strings_to_lon()}, and \code{ddbh3_strings_to_lat()}.
}
\details{
The four functions differ only in the output format:
\itemize{
\item \code{ddbh3_strings_to_bigint()} converts H3 indexes to unsigned 64-bit integers (\code{UBIGINT})
\item \code{ddbh3_strings_to_spatial()} converts H3 indexes to spatial hexagon polygons
\item \code{ddbh3_strings_to_lon()} extracts the longitude of the H3 cell centroid
\item \code{ddbh3_strings_to_lat()} extracts the latitude of the H3 cell centroid
}
}
\examples{
\dontrun{
## TODO
}
}
